# Keep cargo's artifacts under the repository-root build/ directory.
# Relative paths in a cargo config file resolve against this file's directory,
# so ../../build/extract is <repo>/build/extract.
[build]
target-dir = "../../build/extract"
