{
  "schema_version": 1,
  "_comment": "所有 profile 共用的兼容执行参数。Kotlin 先载入本文件，再应用内置 profile、用户 override 和界面核心选择；S08 才会让 Native 消费这些调优值。",
  "_field_comments": {
    "recommended_cpus": "推荐核心编号；当前界面显式选择优先。",
    "heap": "heap 准备阶段的最大尝试次数和超时，时间单位写在字段名中。",
    "race": "三条路线共享的竞态等待与轮询间隔。",
    "stages": "W1/W2/W3 的尝试次数和每轮稳定等待。",
    "routes": "各攻击链独有的尝试次数、调用批量和时序。",
    "handoff": "提权结果交给 KernelSU/模块加载及 enforcing 检查时的轮询参数。"
  },
  "execution": {
    "recommended_cpus": {
      "main": 0,
      "consumer": 1
    },
    "heap": {
      "prepare_max_attempts": 4,
      "prepare_timeout_ms": 240000,
      "kernelsnitch_timeout_ms": 60000
    },
    "race": {
      "route_wait_ms": 1000,
      "setup_settle_us": 50000,
      "state_poll_interval_us": 1000
    },
    "stages": {
      "w1_attempts": 15,
      "w1_settle_us": 100000,
      "w1_scratch_repair_attempts": 3,
      "w2_attempts": 15,
      "w2_settle_us": 100000,
      "w3_chain_rounds": 3,
      "w3_attempts": 6,
      "w3_settle_us": 50000
    },
    "routes": {
      "tcp_zerocopy": {
        "attempts": 2000,
        "arm_sequence": 16,
        "post_receive_hold_iterations": 20000
      },
      "select_stack": {
        "enter_delay_us": 50000,
        "timeout_us": 200000,
        "consumer_max_calls": 1,
        "consumer_burst_calls": 1
      },
      "multicast_waiter": {
        "ready_timeout_ms": 10000,
        "post_requeue_settle_us": 200000,
        "post_adjust_settle_us": 100000
      }
    },
    "handoff": {
      "pre_dispatch_settle_ms": 2000,
      "module_poll_attempts": 30,
      "module_poll_interval_ms": 100,
      "enforce_poll_attempts": 200,
      "enforce_poll_interval_ms": 100
    }
  }
}
