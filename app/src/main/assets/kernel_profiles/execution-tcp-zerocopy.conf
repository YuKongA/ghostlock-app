# GhostLock execution tuning for the tcp_zerocopy route (HOCON); include it
# only from profiles whose route (or fallback) is tcp_zerocopy.
execution {
  routes {
    tcp_zerocopy {
      attempts = 2000
      arm_sequence = 16
      post_receive_hold_iterations = 20000
    }
  }
}
