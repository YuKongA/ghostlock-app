# CPP12r 真机门禁：Multicast（main 拆分 + namespace 系列）W1 panic ×2 — FAIL（回退）

- 构建：APK 283；native `1e196eb90a79ab82aa69d6fa2e3b76462ddc2ed2ec31ce62c97ad4119e4a23f6`
  （`1ec96ab` main 解析 → `172062d` stage 拆分 → `d9b1042` W2/W3 拆分+enum → `300d3bf` 分 TU →
  `d862a51` 四单元 namespace → `fb8fb59`/`dfd0d60`/`a30e8f2` support/route/memory namespace）
- 运行：两次冷机（07:44、08:04），均由用户报告 W1 panic；`uptime` 归零、KernelSU 未加载
- 日志：`CPP12r-20260918-w1-panic.native.log`（65 行，第二次运行，崩溃处截断）；
  两次 Kotlin direct log（`ghostlock-20260918-074423`/`-080413`）均为 0 字节
- 结论：**FAIL（2/2 panic）→ 按 `CPP12k` 先例回退**

## 崩溃位置

- `W1: SELinux attempt 1/1`（target `0xffffff802adaad88`，mode=1 leaf=0），spray 全链成功：
  `mm spray + kernelsnitch ready (cpu=8) +304ms`、`futex collisions found +2926ms`、
  `mm_struct leaked=0xffffff88d4131400 +3015ms`、`prepare_kernel_page ok attempt=1 +3331ms`、
  `heap spray done T+3574ms` → 之后截断（PI route 状态未打印）。
- 与 `CPP12p`（SESSION-02 首次门禁）崩溃位置相同；区别在于 `CPP12q` 隔离复跑 PASS 而本次 2/2 panic。

## 判定与回退

- 布局敏感规则：同一构建连续两次冷机 panic，按 `CPP12k` 先例判定不通过。
- 回退范围：`1ec96ab..a30e8f2`（main 解析、stage 拆分、分 TU、namespace 系列，均未经设备门禁覆盖），
  保留 `da2ab17`/`faac9ce`（SESSION-02，`CPP12q` PASS + 用户豁免）与警告/clang-tidy 基础设施及全部门禁归档。
- 回退后重建 native = `395c5faf2eb879fa0abb0ea47291c51ba75b54a7c1bdfec53e1335d472e987fe`
  （与 `faac9ce` 代码逐字节一致），主机测试与 lint 全绿。

## 观察

- 该系列相对 `395c5faf` 的唯一变化是**代码分层与命名空间**（无逻辑改动）；`waiter_thread`/`do_one_write`
  指令数不变但编码/调度有差异，`.text` 排布整体变化。`KERNEL-PANIC-01` 的间歇性无法完全排除，
  但 2/2 与回退版的复跑结果是可判定的对比。
- 后续如需重试分层：建议按"分 TU → namespace"分更小批次，每批独立门禁，避免再次 2/2 归因困难。
