# CPP12x 真机门禁：route 拆分（native `92780d48…`）W1 panic — 同型 KERNEL-PANIC-01

- 构建：APK 300；native `92780d4875276b104ea50915948d7a6a96d8a464daf05309fd282508c86c0f73`
  （`4c9c46f` 将 `route_operations.cpp` 按 Multicast/TCP/Select 拆分；**8/8 攻击关键函数与
  `CPP13b` 的 `66f0a8a3…` strict 一致**）
- 运行：用户冷启动后第一次 Direct 执行，设备 kernel panic 自动重启（`bootreason=kernel_panic`）
- 日志：`CPP12x-20260918-w1-panic.dmesg.log`（pstore `dmesg-ramoops-0`，完整 Oops）、
  `.native.log`（109 行，止于 W1 `heap spray done T+14090ms`）
- 结论：**FAIL（1/1 panic，同型 `KERNEL-PANIC-01`）**；与拆分无因果（攻击函数逐指令一致）

## 崩溃位置

- `pc : rt_mutex_adjust_prio_chain+0x1b0/0x14c0`，`CPU: 1 PID: 26264 Comm: libghostlock.so`；
  `Call trace: rt_mutex_adjust_prio_chain ← rt_mutex_adjust_pi ← __sched_setscheduler
  ← __do_sys_sched_setattr`。
- `x27(waiter->lock)=0`、`x25(next_lock)=0`、`x28=ffffffc03c2dbb18`（waiter 线程内核栈 waiter）、
  `Code: ... (b8bfc368)`——与 `CPP12s` **完全同型**（ghost waiter 的 NULL lock，
  由本进程 `consumer_thread` 的 `sched_setattr` 触发）。
- 窗口：W1 `heap spray done T+14090ms` 后（`CPP12s` 为 `T+6819ms`，同阶段）。

## 判定与处置

- 该构建与 `CPP13b` PASS 的 `66f0a8a3` 在 8 个攻击关键函数上**逐指令一致**，
  panic 再次证明 `KERNEL-PANIC-01` 与该层布局无因果；同时说明"攻击函数一致"也不足以
  免除设备窗口风险（`CPP12s` 已证明，本次为第二个同型实例）。
- 处置：回退拆分，恢复 `66f0a8a3` 为最终构建；拆分代码保留在历史（`4c9c46f`），
  登记为待环境/攻击层手段（dmesg/pstore 已具备）后重试。
