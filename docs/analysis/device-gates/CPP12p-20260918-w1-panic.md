# CPP12p 真机门禁：Multicast（SESSION-02）W1 panic — FAIL（第一次冷机）

- 构建：APK 274；native `395c5faf2eb879fa0abb0ea47291c51ba75b54a7c1bdfec53e1335d472e987fe`
- 入口：Direct；设备运行中 kernel panic 并自动重启（用户报告 "w1 panic"；`uptime` 归零、KernelSU 未加载）
- 日志：`CPP12p-20260918-w1-panic.native.log`（65 行，崩溃处截断）；Kotlin 侧 direct log（00:41:47）为 0 字节
- 结论：**FAIL（第一次冷机）**；归因未定，按 `CPP12l` 流程隔离复跑

## 崩溃位置

- `W1: SELinux attempt 1/1`（target `0xffffff802adaad88`，mode=1 leaf=0）。
- spray 全链成功：`mm spray + kernelsnitch ready (cpu=8) +446ms`、`early collision screen 14/7 at 31%`、`futex collisions found +3163ms`、`mm_struct leaked=0xffffff88cf457000 +3312ms`、`payload ready +3318ms`、`prepare_kernel_page ok attempt=1 +3551ms`、`heap spray done T+3853ms`。
- 崩溃发生在 `heap spray done` 之后、`multicast route status` 之前：即 W1 的 **PI route 阶段**（waiter/owner/consumer 线程与内核 strip/spray 窗口），与 `KERNEL-PANIC-01` 记录的攻击层窗口一致。
- 无 route status、无 fallback、无 handoff 日志。

## 归因分析

- SESSION-02 改动涉及的函数在崩溃点均未执行：victim 相关代码最早在 W2 `spawn_victim` 运行；`~ExploitSession` 在进程退出时运行；`retry_write_stage` 的 session 参数只影响 W1 调用约定。
- 本批 `.text` 重排（`~ExploitSession` 150→238、`child_main` 406→413、`run_exploit`/`retry_write_stage` 签名与布局变化）可能改变 LTO 内联/寄存器分配，但崩溃点位于攻击层已知窗口。
- 先例：`PI-TIMEOUT-01` 批次在首条 W1 route 前/中 panic（`CPP12-20260917b/c`）；`CPP06c`/`CPP07`/`CPP12k` 亦有 spray/route 窗口 panic；`CPP12l` 证明隔离复跑可判定归因。

## 下一步

- **隔离复跑结果（`CPP12q-20260918-multicast-pass`）**：同一构建 `395c5faf…` 复跑完整 PASS（6/6 route clean、T+28231ms、`KernelSU ready`、无重启），符合 `KERNEL-PANIC-01` 的间歇性特征。
- 仍需第二次冷机（KernelSU 加载后 reboot 回干净启动）以补足布局敏感连续复跑要求；若第二次 PASS 则 SESSION-02 通过，再次 panic 则回退。
