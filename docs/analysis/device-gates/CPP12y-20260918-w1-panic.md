# CPP12y 真机门禁：同一构建（`66f0a8a3…`）复跑 panic — refcount WARN 分支

- 构建：APK 302；native `66f0a8a3b6450b9c50a02759bcb77162cb3b56af9bb3febcbbac161593184ea6`
  （**与 `CPP13b` PASS 的构建逐字节相同**）
- 运行：用户在一次执行中 panic（`ro.boot.bootreason=kernel_panic,bug`），设备重启；
  重连后再次执行成功（`CPP13b` 补记第二次冷机）
- 日志：`CPP12y-20260918-w1-panic.dmesg.log`（pstore `dmesg-ramoops-0`，完整 Oops）
- 结论：**同构建 PASS/panic 直接对照**（`CPP13b` PASS → 本页 panic → 随后 PASS）；
  `KERNEL-PANIC-01` 的环境/时序性质最终确认

## 崩溃位置

- `pc : rt_mutex_adjust_prio_chain+0x14b0/0x14c0`，`CPU: 1 PID: 15570 Comm: libghostlock.so`；
  `Call trace` 同 `CPP12s`/`CPP12x`（`rt_mutex_adjust_prio_chain ← rt_mutex_adjust_pi
  ← __sched_setscheduler ← sched_setattr`）。
- 故障指令 `brk #0x800`（WARN/BUG 断点）；镜像反汇编（`_text+0x1778db4`）显示前置流：
  `mov w10,#0xc0000000; str w10,[x8]`（`REFCOUNT_SATURATED`）+ `bl printk`（warn 输出），
  即 `put_task_struct()` 内 `refcount_dec_and_test` 的 **refcount underflow/saturate** 路径
  （`refcount_warn_saturate`），在 `panic_on_warn` 下升级为 panic。
- 与 `CPP12s`/`CPP12x` 的 NULL-lock 是同一 primitive 的不同表现：ghost waiter/PI 遍历
  破坏了 task 的 `usage` 计数（本次）或 waiter 的 `lock`（前两次）。

## 判定

- 同一构建 `66f0a8a3` 同日：`CPP13b` PASS（09:19）→ 本页 panic → 重连后 PASS（第二次冷机）。
- 构建无需回退（已有 PASS 且本次无代码变化）；设备窗口证据已充分（三个同型 panic 实例
  覆盖两个构建、两个不同崩溃形态）。
