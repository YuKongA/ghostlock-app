# PD2361 5.15.178 人工反编译几何候选（2026-09-26）

> 对象：PD2361 OTA `payload.bin` 解出的 `boot.img`，release
> `5.15.178-g3575c47dc7ce-dirty`（未验证，无 `-android13-` 标签）。
> 目的：用反汇编人工补齐 extractor 因“未验证”而省略的 multicast 几何。
> **结论是候选值，不是已验证 profile；`waiter_off`、伪造布局与 phys 必须真机确认。**

## 方法

- `tools/extract_rs/examples/disasm_func.rs`（临时逆向工具）：`BootImage` + 内嵌 kallsyms 恢复
  + `disasm::disassemble_range`，按符号打印反汇编。
- 参照公开 `GhostLockAdapt` 的 `exploit-finale/docs/WRITE-PRIMITIVES.md` 帧深算法。

## setsockopt 链（IPv4 MCAST_BLOCK_SOURCE stamp）

| 函数 | 帧大小 | 证据 |
|---|---:|---|
| `__arm64_sys_setsockopt` | `0x10` | `stp x29,x30,[sp,#-0x10]!` |
| `__sys_setsockopt` | `0x80` | `sub sp,sp,#0x80` |
| `sock_common_setsockopt` | `0x40` | `sub sp,sp,#0x40`（CFI 检查内联，无额外帧） |
| `udp_setsockopt` | `0x10` | `stp x29,x30,[sp,#-0x10]!`，`bl ip_setsockopt` |
| `ip_setsockopt` | `0x290` | `stp x29,x30,[sp,#-0x60]!` + `sub sp,sp,#0x230` |
| **合计** | **`0x370`** | — |

- `ip_setsockopt`：`add x0, sp, #0x18` + `mov w2, #0x108` → `greqs = sp+0x18`，拷贝窗口 `0x108`。
- greqs 深度 = `0x370 − 0x18 = 0x358`。

## futex 链（waiter 分配）

| 函数 | 帧大小 | 证据 |
|---|---:|---|
| `__arm64_sys_futex` | `0xa0` | `sub sp,sp,#0xa0`；`bl do_futex` |
| `do_futex` | `0xd0` | `sub sp,sp,#0xd0`；`bl futex_wait_requeue_pi` |
| `futex_wait_requeue_pi` | `0x1c0` | `sub sp,sp,#0x1c0`；`add x9, x27, #0x18`，`x27=sp+0x28` |
| **合计** | **`0x330`** | — |

- waiter 栈局部：`x27 = sp+0x28`；`x9 = waiter+0x18` 与 BTF `rt_mutex_waiter.pi_tree_entry=0x18`
  一致 → waiter `sp+0x28`。
- waiter 深度 = `0x330 − 0x28 = 0x308`。

## 推导结果（候选）

| 字段 | 值 | 依据 | 置信度 |
|---|---:|---|---|
| `compact_waiter` | `1` | BTF `sizeof(rt_mutex_waiter)=0x58` | 高（镜像事实） |
| `task_offset` | `48` | BTF `rt_mutex_waiter.task` | 高（镜像事实） |
| `lock_offset` | `56` | BTF `rt_mutex_waiter.lock` | 高（镜像事实） |
| `buffer_size` | `264` (`0x108`) | `ip_setsockopt` 拷贝窗口 | 高（镜像事实） |
| `waiter_off` | `80` (`0x50`) | `0x358 − 0x308`，futex(1 帧)→stamp(1 帧) | **中；须真机 PROBE 复核** |
| `kernelsnitch.collisions` | `8` | 沿用 GKI 5.15 实测族默认 | 低（经验） |
| `kernelsnitch.mm_struct_sz` | `1024` | BTF `sizeof(mm_struct)=0x3e0`，SLUB stride 对齐 `0x400` | 中；须真机核实 |

## 无法静态补齐（必须真机）

| 字段 | 原因 |
|---|---|
| `fake_lock_offset` / `fake_task_offset` / `lock_slots_offset` / `lock_slot_count` / `lock_slot_stride` | 是相对 BSS 符号 `z_pagemap_global`（`offset.mcast_fake_bss`）的**伪造对象落点**，取决于该符号周围数据段布局、页可写性与对齐；A301SO 常量（`0x1200/0x3200/0x80/12/8`）未获跨设备佐证，不能外推。 |
| `kernel_phys_load` | 不在 `boot.img`/DT；`xbl_config` FDT 未解析出唯一 NOMAP/Kernel map。需设备 `/proc/iomem` 的 “Kernel code”（`0x…0000`）。 |
| `waiter_off` | 公开方法明确要求按 `MCAST_PROBE`（panic 后读 `x27`）逐机确认；静态推导仅给候选。 |

## 参考

- 公开帧深算法与 PROBE：
  `github.com/AnonymousUser369/GhostLockAdapt` → `exploit-finale/docs/WRITE-PRIMITIVES.md`。
- 本地工具：`tools/extract_rs/examples/disasm_func.rs`。

## 后续

- 用候选值补全 `~/Downloads/5.15.178-g3575c47dc7ce-dirty.conf`（`waiter_off`/`buffer_size`/
  `compact_waiter` + `kernelsnitch`），`fake_*`/`lock_slots_*`/`kernel_phys_load` 留待真机；
  任何真机运行前按 PROBE 复核 `waiter_off`。
